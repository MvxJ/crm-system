{% block body %}
    {% if logoUrl %}
        <img src="{{ logoUrl }}" alt="Logo">
    {% endif %}
    <h1>Your Verification Code!</h1>
    <p>Your verification code is: {{ authCode }}</p>
    <p>Regards,</p>
    <p>CRM App Team</p>
{% endblock %}