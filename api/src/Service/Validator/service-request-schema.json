{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "properties": {
    "isClosed": {
      "type": "boolean"
    },
    "description": {
      "type": "string"
    },
    "customer": {
      "type": "integer"
    },
    "user": {
      "type": "integer"
    },
    "contract": {
      "type": "integer"
    },
    "status": {
      "type": "integer"
    }
  },
  "required": ["customer", "contract"],
  "errorMessage": {
    "required.customer": "Customer is required, Please provide value.",
    "required.contract": "Contract is required, Please provide value.",
    "type.customer": "Customer should be a integer. provide a valid number.",
    "type.contract": "Contract should be a integer. provide a valid number.",
    "type.isClosed": "Is Closed should be a bool. provide a valid number.",
    "type.description": "Description should be a string. provide a valid string.",
    "type.user": "User should be a integer. provide a valid number.",
    "type.status": "Status should be a integer. Provide a valid int value."
  }
}